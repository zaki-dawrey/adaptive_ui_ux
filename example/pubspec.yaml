name: adaptive_ui_ux_example
description: Example app for the adaptive_ui_ux package
publish_to: "none"
version: 1.0.0+1

environment:
    sdk: ^3.5.4
    flutter: ">=3.13.0"

dependencies:
    flutter:
        sdk: flutter
    adaptive_ui_ux:
        path: ../
    cupertino_icons: ^1.0.6

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^4.0.0

flutter:
    uses-material-design: true
